import { Box, styled } from "../../constants/MuiConstants";

export const DashboardContainer = styled(Box)(({}) => ({
  // display: "flex",
  // justifyContent: "space-between",
  // maxWidth:"1200px",
  // width:"100%"
}));
